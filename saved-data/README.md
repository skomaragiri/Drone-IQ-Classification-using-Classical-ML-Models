# Saved Data
This directory holds all the processed numpy array data. 

The pipeline is as follows: 
1. `.sigmf-meta` `.sigmf-data` files 
2. Converted to numpy complex arrays 
3. Saved to as `.npy` files in this directory